var path   = require('path');
var mm     = require('./lib');
var fs     = require('fs');
var test   = require('tape');

// var sample = path.join();
mm(fs.createReadStream("/Users/leetreveil/Downloads/11. Sub Focus - You Make It Better (feat. Culture Shock & TC).mp3"), {duration: true})
  .on('metadata', function (result) {
    console.log(result);
  })
  .on('duration', function (result) {
    console.log('BOOM');
    console.log(result);
  })
  .on('done', function (err) {
    if (err) throw err;
  })
